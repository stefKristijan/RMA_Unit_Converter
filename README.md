# RMA_Unit_Converter

Za ovaj zadatak korištene su tri aktivnosti (MainActivity, ConverterActivity i ResultActivity). U MainActivity-ju se klikom na jedan od èetiri dugmeta, definiranih u activity_main.xml datoteci, odreðuje koje æe se jedinice pretvarati (duljina, volumen, temperatura ili masa). To je ostvareno da se u preoptereæenoj funkciji onClick() pomoæu switch-case¬-a koji na temelju id-a dugmeta odreðuje koje jedinice æe se pretvarati. Ovisno o jedinici koja se pretvara kreira se objekt eksplicitnog Intent-a kojim se pokreæe aktivnost ConverterActivity¬. Uz pomoæ metode putExtra() na drugu aktivnost se prosljeðuju dva podatka: polje objekata klase String u kojemu se nalaze odabrane jedinice za pretvorbu te id naslova pretvaraèa.  

U aktivnosti ConverterActivity se u metodi setUI() instanciraju potrebni objekti te se primaju podaci poslani s prve aktivnosti. Naslov pretvaraèa se postavlja u TextView, a polje objekata klase String tj. jedinice za pretvorbu, postavljaju u Spinner-e. Za postavljanje jedinica u Spinner-e koristi se objekt klase ArrayAdapter<> èiji konstruktor prima Context, id layout-a jednog item-a jedinice te polje objekata klase String tj. jedinice za pretvaranje (https://www.mkyong.com/android/android-spinner-drop-down-list-example/). U layout-u je definirana datoteka spinner_item.xml koja opisuje kako æe se prikazati odabrana jedinica u Spinner-u. Metodom setDropDownViewResource() objekta klase ArrayAdapter<> se postavlja izgled jednog retka Spinner-a tj. jedne prikazane vrijednosti. U preoptereæenoj metodi onClick() prvo se provjerava je li korisnik upisao vrijednost u EditText te se ispisuje odgovarajuæa poruka ako korisnik nije upisao nikakvu vrijednost, a ako je vrijednost upisana kreira se novi eksplicitni Intent kojim se pokreæe aktivnost ResultActivity s ciljem prikazivanja rezultata. Prosljeðuju se jedinice koje se pretvaraju, upisana vrijednost i naslov pretvaraèa. Metodom startActivityForResult() se predaje intent i jedan int koji govori o kojem zahtjevu se radi. U preoptereæenoj metodi onActivityResult() se prilikom vraæanja iz treæe aktivnosti prikazuje Toast poruka u kojoj piše da je pretvorba bila uspješna. Ovo je napravljeno zato što sam protumaèio da se u zadatku traži da se kreira aktivnost koja vraæa odreðeni rezultat, pa se prilikom vraæanja iz treæe aktivnosti vraæa RESULT_OK i ispisuje poruka.

U aktivnosti ResultActivity se na temelju primljenih podataka iz prethodne aktivnosti raèuna rezultat pretvaranja. To je ostvareno pomoæu klase ConvertFunctions, u kojoj su sve metode za pretvaranje iz jedne jedinice u drugu statiène (static) kako bi im se moglo pristupati bez kreiranja objekata. Temelj pretvaranja je da se za svaki tip pretvaranja odabere jedna, glavna, jedinica u koju se pretvara i iz koje se pretvara u odabranu jedinicu. Ako se pretvara duljina, glavna jedinica je metar, pa æe se ukoliko korisnik želi pretvoriti vrijednost iz centimetara u kilometre prvo vrijednost u centimetrima pretvoriti u metre, a zatim iz metara u kilometre.  Za vraæanje rezultata RESULT_OK prilikom pritiska dugmeta za povratak potrebno je preopteretiti metodu onBackPressed(). 

Za kompletan izgled aplikacije su korištene pozadinske slike za dugmad i opæenito pozadinu aplikacije. Za svako dugme je napravljena .xml datoteka u kojoj je definiran izgled dugmeta i kako æe se dugme ponašati prilikom klika (http://stackoverflow.com/questions/15333529/how-to-provide-shadow-to-button). Stil objekata suèelja koji se ponavljaju u aplikaciji su definirani u datoteci styles.xml da se ne treba svaki put pisati isti kod. U datoteci strings.xml su definirani svi string-ovi te i polje string¬-ova sa jedinicama za pretvaranje. Kako bi se aplikacija mogla koristiti i u landscape-u bilo je potrebno napraviti mape layout-land u kojoj se definira slièan layout kao i kod portret moda rada te mapu drawable-land u kojoj se nalaze slike koje se trebaju promijeniti prilikom rada u landscape modu. 

Nisam se susreo s nikakvim problemima samo mi nije bilo jasno zašto se zadnja aktivnost trebala pozivati za rezultat ako se rezultat prikazuje u njoj.